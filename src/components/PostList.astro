---
import FormattedDate from "./FormattedDate.astro"
import Link from "./Link.astro"
import type { CollectionNames } from "../utils.ts"
import { getSortedCollection } from "../utils.ts"

type Props = {
  collectionName: CollectionNames
}
const { collectionName } = Astro.props

const posts = await getSortedCollection(collectionName)
---

<ul class="space-y-8">
  {
    posts.map((post) => (
      <li class="flex flex-col">
        <Link
          href={`/${collectionName}/${post.id}`}
          title={post.data.title}
          titleClass="text-primary font-bold text-xl"
        >
          <FormattedDate class="text-sm" date={post.data.date} />
        </Link>
      </li>
    ))
  }
</ul>
